{
  "name": "hairnailai-backend",
  "version": "1.0.0",
  "description": "Thin backend for HairNail.ai — proxies LightX hair-swap requests so the API key never ships inside the Android app.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1"
  }
}
